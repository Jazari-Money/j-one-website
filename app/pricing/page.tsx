import { PricingPage } from "./PricingPage";

export const metadata = {
  title: "Pricing — Jazari One",
  description: "Simple preview pricing for Jazari accounts, cards, stablecoins, and local payouts.",
};

export default function Page() {
  return <PricingPage />;
}
