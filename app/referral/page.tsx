import { ReferralPage } from "./ReferralPage";

export const metadata = {
  title: "Accept your invite — Jazari One",
  description: "Add your phone number to accept your Jazari One referral invite.",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <ReferralPage />;
}
